<LinearLayout
    style="@style/StandardLinearLayout"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <TextView
        style="@style/MarginInfo"
        android:id="@+id/paymentDetails"
        android:layout_width="wrap_content"/>

    <Button
        style="@style/TransButton"
        android:id="@+id/paymentHash"/>

    <Button
        style="@style/TransButton"
        android:id="@+id/paymentProof"
        android:text="@string/ln_copy_proof"
        android:visibility="gone"/>

    <Button
        style="@style/TransButton"
        android:id="@+id/paymentRetryAgain"
        android:text="@string/ln_retry_again"
        android:visibility="gone"/>

    <View style="@style/GrayDivider"/>

</LinearLayout>